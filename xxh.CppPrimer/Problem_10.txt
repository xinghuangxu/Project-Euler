#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int isPrime(int num);

int main()
{
	double sum=0;
	for(int i=2;i<2000000;i++){
		if(isPrime(i)){
			sum+=i;
		}
	}
	cout.precision(15);
	cout<<"Result:"<<sum;
	cout<<endl;
	cin.get();
	cin.get();
	return 0;
}

int isPrime(int num)
{

	for(int i=2;i<=sqrt((double)num);i++){
		while(num%i==0){
			return 0;
		}
	}
	return 1;
}

